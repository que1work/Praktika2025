import React from 'react';
import '../App.css';

const Header = () => {
    return (
        <div className="finish">
        <header>
            <p>FLAT UI COLORS</p>
        </header>
    </div>
    );
};

export default Header;
